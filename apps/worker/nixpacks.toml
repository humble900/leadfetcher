[phases.setup]
nixPkgs = ["nodejs_20", "npm-9_x"]

[phases.install]
cmds = ["cd ../.. && npm install"]

[phases.build]
cmds = ["cd ../.. && npm run build -w packages/shared && npm run build -w apps/worker"]

[start]
cmd = "node dist/worker.js"

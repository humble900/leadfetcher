{
  "name": "leadfetcher",
  "version": "1.0.0",
  "private": true,
  "description": "Production-grade multi-tenant lead extraction platform",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev:api": "npm run dev -w apps/api",
    "dev:worker": "npm run dev -w apps/worker",
    "dev:dashboard": "npm run dev -w apps/dashboard",
    "build": "npm run build:shared && npm run build -w apps/api && npm run build -w apps/worker && npm run build -w apps/dashboard",
    "build:shared": "npm run build -w packages/shared",
    "build:api": "npm run build -w packages/shared && npm run build -w apps/api",
    "build:worker": "npm run build -w packages/shared && npm run build -w apps/worker",
    "build:dashboard": "npm run build -w packages/shared && npm run build -w apps/dashboard",
    "start:api": "node apps/api/dist/server.js",
    "start:worker": "node apps/worker/dist/worker.js",
    "start:dashboard": "npm run start -w apps/dashboard",
    "db:migrate": "npm run db:migrate -w apps/api",
    "db:seed": "npm run db:seed -w apps/api"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "devDependencies": {
    "railway": "^3.1.1",
    "ts-node": "^10.9.2",
    "tsx": "^4.22.4"
  }
}
